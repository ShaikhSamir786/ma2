# AGENTS.md

Pointer file. The canonical global context for this workspace lives in
`ai-agent/rules.md` — read that file first and follow it. It covers the
Universal Engineering Orchestrator Suite layout, the mandated workflow
(engineering-orchestrator → repo-analyzer → domain skill(s) → verifier →
code-reviewer), the Node.js/TypeScript stack rule, verification rule, safety
boundaries, and the MCP server config.
